---
title: Overview & Authentication
sidebarTitle: Overview & Authentication
description: Using the Vantik API
---

## Installation

The API is available to run CRUD operations on Vantik.

<CodeGroup>

```bash npm
npm i @vantikhq/sdk@0.1.3
```

```bash pnpm
pnpm add @vantikhq/sdk@0.1.3
```

```bash yarn
yarn add @vantikhq/sdk@0.1.3
```

</CodeGroup>

## Usage

```ts
import { configure, createIssue } from "@vantikhq/sdk";

configure({
  // this is the default and if the `VANTIK_TOKEN` environment variable is set, can omit calling configure
  token: <token>>,
});

async function main() {
  const runs = await createIssue({
    title: "Issue title",
    description: "Issue description",
  });
}

main().catch(console.error);
```
